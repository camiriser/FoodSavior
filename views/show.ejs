<!DOCTYPE html>
<html>
    
    <head>
        <title>Food_Savior</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <link href="https://fonts.googleapis.com/css?family=Philosopher" rel="stylesheet">
        
    </head>
    
   <body style="background-image:linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('https://images.unsplash.com/photo-1549590143-d5855148a9d5?ixlib=rb-1.2.1&auto=format&fit=crop&w=1300&q=80');background-size:cover;width:100vw;height:100vh;">
    <nav class="navbar navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">Food Savior</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <% if(!currentUser){ %>
                        <li><a href="/login">Login</a></li>
                        <li><a href="/register">Sign Up</a></li>
                    <% } else { %>
                        <li><a href="#"><%= currentUser.username %></a></li>
                        <li><a href="/logout">Logout</a></li>
                     <% } %>
                </ul>
            </div>
        </div>
    </nav>

       
        <header class="jumbotron" style="background: rgba(255, 255, 255, 0.3)">
             <div class="container">
                 <h1 style="color: white;">Full Recipe!!!</h1>
                 <p style="color: white;"> Time to start cooking!</p>
             </div>
        </header>
    
         <div id="container" class="jumbotron" style="background: rgba(255, 255, 255, 0.3); width: 70%; margin: 0 auto 0 auto ;">
        
                <% data%>
                 
                    <h1 style="color: white; width: 100%; margin: 0 auto 0 auto;"><%= data["title"] %></h1>
                 
                <div class="caption" style="width: 60%; margin: 0 auto 0 auto;">     
                    <img src="<%=data["image"]%>">
                </div>
                
                  
                <div class="caption" style="width: 60%; margin: 50px auto 50px auto;">  
                    <p align="justify" style="color: white; font-size: 150%;"><%=data["instructions"]%></p>
                </div>
                
                     <a href="/search" class="btn btn-success" style="margin: 0 auto 0 auto;">Search Again!</a>
            </div>
   

<% include ./partials/footer %>